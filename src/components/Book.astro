---
const { img_src, book_title, author, final_score } = Astro.props;
---

<div class="flex flex-row gap-10 p-10 w-full items-center place-content-center">
    <img class="h-96" alt=`Cover image for ${book_title}` src={img_src} />
    <div class="flex flex-col min-w-1/3 w-2/4">
        <h2 class="text-2xl">{book_title}</h2>
        <span>by {author}</span>
        <span class="pt-2">Final Score: {final_score}</span>
    </div>
</div>

